const add = (n1, n2) => {
  return n1 + n2;
};

const iskuqaybin = (n1, n2) => {
  return n1 / n2;
};

const kalajarid = (n1, n2) => {
  return n1 - n2;
};

export default kalajarid;
//export default add

export { add, iskuqaybin };
